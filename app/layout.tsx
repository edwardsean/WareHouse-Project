import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dajia (大家)",
  description: "Warehouse Management System (WMS)",
  // Can edit the icon by changing the favicon.ico and apple-touch-icon.png files in the public folder
  icons: {
    icon: "/favicon.ico",        // Regular favicon for browsers
    shortcut: "/favicon.ico",    // Shortcut icon for Windows
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
